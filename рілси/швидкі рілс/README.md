# швидкі рілс

Тут зберігаються матеріали для рілсів цієї теми (відео, фото).

Список матеріалів і теги для агента — у `manifest.yaml` поруч. Додаючи
новий файл сюди, обов'язково впишіть його в `manifest.yaml`, інакше
агент його не побачить.
